@model IList<YangKai.BlogEngine.Modules.PostModule.Objects.Channel>
<div id="channelbox" class="postbox">
    <h3>频道</h3>
    <div class="inside">
    <ul id="channel-tabs" class="channel-tabs">
        @foreach (var channel in Model)
        {
        <li id="channel-tab-@channel.Url"><a href="JavaScript:void(0);">@channel.Name</a></li>
        }
    </ul>
    @foreach (var channel in Model)
    {
    <div id="mainclass-@channel.Url" class="tabs-panel mainclass-panel">
		<ul class="checklist">
            @foreach (var mainclass in channel.Groups)
            {
            <li><label><input type="radio" value="@mainclass.Url" name="mainclassRadio" />@mainclass.Name</label></li>
            }
	    </ul>
	</div>
    }
    </div>
</div>